<div>
  <h1 class="font-bold text-2xl mb-3">Editing Project</h1>

  <div id="<%= dom_id @project %>">
    <%= render "form", project: @project %>
    <%= link_to "Cancel", projects_path, class: "btn mb-3 bg-gray-100" %>
  </div>
</div>
